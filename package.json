{
  "name": "coronavirus-sms-updates",
  "version": "1.0.0",
  "description": "Sends SMS updates with latest Coronavirus(COVID-19) statistics.",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node ./dist/index.js",
    "start:dev": "ts-node ./src/index.ts"
  },
  "keywords": [
    "coronavirus",
    "sms",
    "covid-19"
  ],
  "author": "edwinork",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^13.9.0",
    "prettier": "^1.19.1",
    "rimraf": "^3.0.2",
    "ts-node": "^8.6.2",
    "typescript": "^3.8.3"
  }
}
